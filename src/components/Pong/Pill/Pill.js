import React from 'react';
import PropTypes from 'prop-types';
import './Pill.css';

const Pill = () => (
  <div className="Pill" data-testid="Pill">
    Pill Component
  </div>
);

Pill.propTypes = {};

Pill.defaultProps = {};

export default Pill;
